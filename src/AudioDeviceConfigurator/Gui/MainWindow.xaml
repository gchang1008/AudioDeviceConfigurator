<Window x:Class="AudioDeviceConfigurator.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AudioDeviceConfigurator" Height="320" Width="480">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Playback endpoint" Margin="0,0,0,4" />
        <ComboBox Grid.Row="1" x:Name="EndpointCombo"
                  DisplayMemberPath="FriendlyName"
                  SelectionChanged="EndpointCombo_OnSelectionChanged" />

        <TextBlock Grid.Row="2" Text="Speaker channels" Margin="0,8,0,4" />
        <ComboBox Grid.Row="3" x:Name="ChannelCombo"
                  SelectionChanged="ChannelCombo_OnSelectionChanged">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Grid.Row="4" Text="Default format" Margin="0,8,0,4" />
        <ComboBox Grid.Row="5" x:Name="FormatCombo"
                  DisplayMemberPath="DisplayText"
                  SelectionChanged="FormatCombo_OnSelectionChanged" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,12,0,0">
            <Button x:Name="ApplyButton" Content="Apply" Click="ApplyButton_OnClick" />
            <Button x:Name="PlayButton" Content="Play" Click="PlayButton_OnClick" />
            <Button x:Name="StopButton" Content="Stop" Click="StopButton_OnClick" />
        </StackPanel>

        <TextBlock Grid.Row="7" x:Name="StatusText" VerticalAlignment="Bottom"
                   TextWrapping="Wrap" />
    </Grid>
</Window>